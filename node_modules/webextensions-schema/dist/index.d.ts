import { WebExtensionsSchemaOptions, Schema, SchemaRaw, SchemaNamespaces } from './types';
export declare class WebExtensionsSchema {
    private _schemas;
    private _tag;
    constructor({ schemas, tag }: {
        schemas: Schema;
        tag: string;
    });
    raw(): SchemaRaw;
    namespaces(): SchemaNamespaces;
    tag(): string;
}
declare const webExtensionsSchema: (options?: WebExtensionsSchemaOptions) => Promise<WebExtensionsSchema>;
export default webExtensionsSchema;
export * from './types';
