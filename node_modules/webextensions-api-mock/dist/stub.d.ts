import { SchemaNamespaces } from 'webextensions-schema';
declare type BrowserOut = any;
export declare class BrowserGenerator {
    private sandbox;
    private aliases;
    private browser;
    private namespaces;
    constructor(namespaces: SchemaNamespaces);
    out(): BrowserOut;
}
export {};
